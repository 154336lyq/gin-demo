CREATE DATABASE IF NOT EXISTS web3lite CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE USER IF NOT EXISTS 'web3'@'%' IDENTIFIED BY '123456';
GRANT ALL PRIVILEGES ON web3lite.* TO 'web3'@'%';
FLUSH PRIVILEGES;

USE web3lite;

CREATE TABLE IF NOT EXISTS accounts (
  address VARCHAR(66) PRIMARY KEY,
  balance VARCHAR(100) NOT NULL,
  nonce BIGINT UNSIGNED NOT NULL,
  chain_id BIGINT UNSIGNED NOT NULL,
  updated_at DATETIME NOT NULL
);

CREATE TABLE IF NOT EXISTS transactions (
  hash VARCHAR(80) PRIMARY KEY,
  from_addr VARCHAR(66) NOT NULL,
  to_addr VARCHAR(66) NOT NULL,
  value VARCHAR(100) NOT NULL,
  block_number BIGINT UNSIGNED NOT NULL,
  gas BIGINT UNSIGNED NOT NULL,
  nonce BIGINT UNSIGNED NOT NULL,
  success BOOLEAN NOT NULL,
  chain_id BIGINT UNSIGNED NOT NULL,
  confirmations BIGINT UNSIGNED NOT NULL,
  state INT NOT NULL,
  updated_at DATETIME NOT NULL,
  INDEX idx_from_addr (from_addr),
  INDEX idx_to_addr (to_addr)
);

CREATE TABLE IF NOT EXISTS users (
  user_id VARCHAR(64) PRIMARY KEY,
  name VARCHAR(120) NOT NULL,
  email VARCHAR(160) NOT NULL UNIQUE,
  chain_id BIGINT UNSIGNED NOT NULL,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL
);

CREATE TABLE IF NOT EXISTS user_accounts (
  user_id VARCHAR(64) NOT NULL,
  address VARCHAR(66) NOT NULL,
  chain_id BIGINT UNSIGNED NOT NULL,
  created_at DATETIME NOT NULL,
  PRIMARY KEY(user_id, address),
  INDEX idx_ua_address(address)
);

CREATE TABLE IF NOT EXISTS event_logs (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  tx_hash VARCHAR(80) NOT NULL,
  block_number BIGINT UNSIGNED NOT NULL,
  chain_id BIGINT UNSIGNED NOT NULL,
  contract_address VARCHAR(66) NOT NULL,
  topic0 VARCHAR(120) NOT NULL,
  topics_json TEXT NOT NULL,
  data TEXT NOT NULL,
  log_index BIGINT UNSIGNED NOT NULL,
  event_name VARCHAR(120) NOT NULL,
  updated_at DATETIME NOT NULL,
  INDEX idx_event_filter(chain_id, contract_address, topic0, id)
);

INSERT INTO accounts(address, balance, nonce, chain_id, updated_at)
VALUES
  ('0x1111111111111111111111111111111111111111', '5000000000000000000', 0, 1, CURRENT_TIMESTAMP),
  ('0x2222222222222222222222222222222222222222', '3000000000000000000', 0, 1, CURRENT_TIMESTAMP),
  ('0x9999999999999999999999999999999999999999', '1000000000000000000', 0, 1, CURRENT_TIMESTAMP)
ON DUPLICATE KEY UPDATE address = address;

